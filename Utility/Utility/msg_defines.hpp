/**
* @file msg_defines.hpp
*
* @author Hourui (liquidmonkey)
*/
#ifndef __MSG_DEFINES_HPP__
#define __MSG_DEFINES_HPP__

#include <cstdint>
#include <string>
#include "base_defines.hpp"

namespace Utility
{
////////////////////////////////////////////////////////////////////////////////////////////////////
namespace mem{ class message; }
////////////////////////////////////////////////////////////////////////////////////////////////////
namespace msg
{
////////////////////////////////////////////////////////////////////////////////////////////////////
class object_iface;
enum err : int
{
	pack = -3,
	unpack = -2,
	pre_unpack = -1,
	none = 0,
	logic,
	null_handle,
};
using handler_t = int(*)(object_iface*, mem::message*);
struct MS_HEADER
{
	std::uint32_t m_uiLen;
	std::uint32_t m_uiId;
};
static constexpr net_size_t MS_HEADER_LEN = (net_size_t)sizeof(MS_HEADER);
////////////////////////////////////////////////////////////////////////////////////////////////////
}//namespace msg
////////////////////////////////////////////////////////////////////////////////////////////////////
}//namespace Utility 
#endif //__MSG_DEFINES_HPP__